@model FinalProject1.Models.Route_

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Route_</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Route_id" />
            <div class="form-group">
                <label asp-for="Vehicle_Id" class="control-label"></label>
                <select asp-for="Vehicle_Id" class="form-control" asp-items="ViewBag.Vehicle_Id"></select>
                <span asp-validation-for="Vehicle_Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="routes" class="control-label"></label>
                <input asp-for="routes" class="form-control" />
                <span asp-validation-for="routes" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
